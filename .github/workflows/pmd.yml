name: PMD Static Code Analysis

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '23 6 * * 0'

permissions:
  contents: read

jobs:
  pmd-code-scan:
    permissions:
      contents: read
      security-events: write
      actions: read

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'

      - name: Download PMD 7.14.0
        run: |
          curl -L -o pmd.zip https://github.com/pmd/pmd/releases/download/pmd_releases%2F7.14.0/pmd-dist-7.14.0-bin.zip
          unzip -q pmd.zip
          mv pmd-dist-7.14.0 pmd

      - name: Show directory structure
        run: ls -R

      - name: Run PMD
        run: |
          ./pmd/bin/pmd check -d . -f sarif -R rulesets/java/quickstart.xml -r pmd-report.sarif

      - name: Check if SARIF file was generated
        run: |
          if [ ! -f pmd-report.sarif ]; then
            echo "❌ PMD did not generate pmd-report.sarif"
            exit 1
          fi

      - name: Upload SARIF Report
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: pmd-report.sarif
          category: pmd
